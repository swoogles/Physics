# Hand-authored groups, for when you want an exact arrangement.
# Every field is optional and falls back to numShapes / mass / sandbox_width.
#   pos, vel  meters and meters/second
#   spin      fraction of the group's own break-up spin (scalar = about z)
#   radius    the sphere the group's particles are placed in
scenario.type=explicit
scenario.name=explicit-example
scenario.virial_ratio=0.25

group.0=label=core;  pos=0,0,0;         n=12000; mass=3.0e9; radius=1.2e7; spin=0.5;  dispersion=0.2; color=1,1,1
group.1=label=left;  pos=-3.0e7,0,0;    n=5000;  mass=9.0e8; radius=6.0e6; spin=0.3;  dispersion=0.1; color=0,1,1
group.2=label=right; pos=3.0e7,1.0e7,0; n=5000;  mass=9.0e8; radius=6.0e6; spin=-0.3; dispersion=0.1; color=1,0.5,0

# --- scale -------------------------------------------------------------------
numShapes=4000
mass=598910
sandbox_width=5.2e6

# --- integrator --------------------------------------------------------------
dt=auto
frames_per_crossing=400
forceCalculationMethod=octree
parallel=true
octree_theta=0.5
maximum_runtime=150

# --- merging -----------------------------------------------------------------
collision_radius_multiplier=30.0
merge_target_fraction=0.9
merge_target_seconds=120
minimum_merges_per_frame=10

# --- keeping it in frame -----------------------------------------------------
velocity_damping=0.9995
boundary_strength=0.001
boundary_radius_factor=1.5

# On-screen size of a body, in pixels, from the cube root of its mass. Physical
# radius is far below one pixel at these distances, so without this every
# particle draws the same and a big merged body looks like it vanished.
max_point_size=14
